#include <cassert>
#include <string>
#include <iostream>
#include <limits>

using namespace std;

int main()
{
    int planetas;
    int portales;
    cin >> planetas;
    cin >> portales;
    //grafo
    for (int i=0; i <= portales; i++) {
        //cin >> planeta >> portal
    }
    return 0;
}