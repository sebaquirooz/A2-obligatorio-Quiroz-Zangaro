# Ejercicio 5

## Estado actual

El archivo `ejercicio5.cpp` todavia no tiene implementacion.

El `main` contiene solamente:

```cpp
// TODO
return 0;
```

## Complejidad

Como no hay solucion implementada, no hay una complejidad algoritmica real para documentar.

Actualmente el programa solo inicia y termina, por lo que su costo efectivo es:

```text
O(1)
```

pero eso no representa la complejidad del ejercicio cuando se implemente.
